#Installation:
    git clone git://github.com/minuteman3/vimfiles.git ~/.vim

#Create symlinks:
    ln -s ~/.vim/vimrc ~/.vim

#Run install script:
    sh ~/.vim/install.sh

#ALTERNATIVELY (experimental):
    Paste to your shell:
    curl https://raw.github.com/minuteman3/vimfiles/master/bootstrap.sh | sh
